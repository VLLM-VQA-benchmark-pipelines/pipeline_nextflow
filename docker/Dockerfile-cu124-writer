# Базовый образ Dockerfile-cu124
FROM vlmevalkit:v0.2rc1-cu124

# Создние директории для данных
RUN mkdir /Data

# Копирвание скрипта writer.py в контейнер
COPY /scripts/writer.py /workspace/writer.py

# Запуск writer.py при старте контейнера
CMD ["python", "/workspace/writer.py"]